# Core CV & ML
ultralytics>=8.0.0          # YOLOv8 — flower detection model
opencv-python>=4.8.0        # Image capture, preprocessing, optical flow
numpy>=1.24.0
Pillow>=10.0.0
torch>=2.0.0                # PyTorch backend (use torch-lite on RPi4)
torchvision>=0.15.0

# Pixhawk / MAVLink
pymavlink>=2.4.40           # MAVLink protocol — talks to Pixhawk over serial/UDP
dronekit>=2.9.2             # High-level DroneKit flight commands (optional, see note)

# Inference optimization (Raspberry Pi)
onnxruntime>=1.16.0         # ONNX runtime for optimized RPi inference
# tflite-runtime>=2.13.0    # Uncomment for TFLite on RPi

# Dataset / Training utilities
roboflow>=1.1.0             # Download labeled flower datasets from Roboflow
albumentations>=1.3.0       # Data augmentation pipeline
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

# Utilities
pyyaml>=6.0
loguru>=0.7.0               # Structured logging
pandas>=2.0.0               # Telemetry CSV logging
pyserial>=3.5               # Serial port access (UART to Pixhawk)
RPi.GPIO>=0.7.1             # GPIO for servo trigger (RPi only)

# NOTE: On Raspberry Pi 4 (ARM), install torch via:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Or use ONNX export for faster inference:
#   yolo export model=flower_model.pt format=onnx
