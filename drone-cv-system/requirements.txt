# Core CV & ML
ultralytics>=8.0.0          # YOLOv8 — flower detection training + export to ONNX/TFLite
opencv-python>=4.8.0        # Image capture, preprocessing, Lucas-Kanade optical flow
numpy>=1.24.0
Pillow>=10.0.0
torch>=2.0.0                # PyTorch for training only — NOT needed at inference time on RPi
torchvision>=0.15.0         # Same — export model to ONNX/TFLite, then uninstall torch on RPi

# Pixhawk 2.4.8 / MAVLink
pymavlink>=2.4.40           # MAVLink protocol — Raspberry Pi UART (/dev/ttyAMA0) → Pixhawk TELEM2
# dronekit>=2.9.2           # DroneKit (optional); current code uses pymavlink directly

# ── Inference — ordered by preference for Raspberry Pi deployment ───────────
#
# 1. Google Coral USB Accelerator (PRIMARY — ~15-30 FPS, INT8 EdgeTPU model)
#    Hardware: Google Coral USB Edge TPU connected to RPi USB 3.0 port
#    Runtime:  sudo apt install libedgetpu1-std
#    Model:    edgetpu_compiler flower_detector.tflite → flower_detector_edgetpu.tflite
#    Install packages from Coral index:
#      pip install pycoral --extra-index-url https://google-coral.github.io/py-packages/
#      pip install tflite-runtime --extra-index-url https://google-coral.github.io/py-packages/
#    Uncomment when deploying to Raspberry Pi with Coral USB attached:
# pycoral>=2.0.0
# tflite-runtime>=2.13.0
#
# 2. ONNX Runtime CPU (FALLBACK — ~8-12 FPS at 640×640, no special hardware)
onnxruntime>=1.16.0

# Dataset / Training utilities (development machine only, not needed on RPi)
roboflow>=1.1.0             # Download labeled flower datasets from Roboflow
albumentations>=1.3.0       # Data augmentation pipeline
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0

# Utilities
pyyaml>=6.0
loguru>=0.7.0               # Structured logging
pandas>=2.0.0               # Telemetry CSV logging
pyserial>=3.5               # Serial port access (UART to Pixhawk)
RPi.GPIO>=0.7.1             # GPIO for servo fallback (when not using Pixhawk AUX OUT 1)

# ── Raspberry Pi installation notes ─────────────────────────────────────────
# torch/torchvision (ARM wheels):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Disable Bluetooth to free hardware UART for Pixhawk:
#   echo "dtoverlay=disable-bt" | sudo tee -a /boot/config.txt && sudo reboot
# Use /dev/ttyAMA0 at 921600 baud for Pixhawk TELEM2 connection.
